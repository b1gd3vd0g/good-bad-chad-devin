/*
 * Use this file to configure the address of the player account/saving API host.
 * The provided host `https://www.api.goodbadchad.bigdevdog.com` does not permit CORS
 * requests from deployments of this game **other than** the one at 
 * `https://www.goodbadchad.bigdevdog.com`.
 */

const config = {
  apiHost: 'https://www.api.goodbadchad.bigdevdog.com'
};
